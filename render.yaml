services:
  - type: web
    name: len-den
    runtime: node
    region: oregon
    buildCommand: npm install
    startCommand: node --experimental-sqlite server.js
    envVars:
      - key: NODE_VERSION
        value: 22.5.0
